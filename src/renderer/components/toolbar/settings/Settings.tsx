import * as React from 'react';

interface SettingsProps {

}

/**
 * Settings Plugin
 */
export default function Settings(props: SettingsProps) {

    return (
        <section className="plugin settings theme">
            <span>Settings</span>
        </section>
    )
}

